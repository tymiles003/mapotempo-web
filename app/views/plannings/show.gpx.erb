<?xml version="1.0" encoding="UTF-8"?>
<gpx>
  <name><%= @planning.name %></name>
  <time><%= Time.now.strftime("%Y-%m-%d") %></time>
<% @planning.routes.select{ |route| route.vehicle && route.stops.size > 2 }.each{ |route| %>
<%=  render 'routes/show', route: route %>
<% } %>
</gpx>
