<h1><%= t '.title' %></h1>

<p>
  <%= link_to new_planning_path, class:"btn btn-primary btn-sm" do %><i class="icon icon-plus icon-fixed-width"></i> <%= t '.new' %><% end %>
</p>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th><%= t 'activerecord.attributes.planning.name' %></th>
      <th><%= t 'activerecord.attributes.planning.tags' %></th>
      <th><%= t 'activerecord.attributes.planning.created_at' %></th>
      <th><%= t 'activerecord.attributes.planning.updated_at' %></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @plannings.each do |planning| %>
      <tr>
        <td><%= planning.name %></td>
        <td>
        <% planning.tags.each{ |tag| %>
          <%= tag.label %>
        <% } %>
        </td>
        <td><%= l planning.created_at %></td>
        <td><%= l planning.updated_at %></td>
        <td class="text-right">
          <%= link_to planning_duplicate_path(planning), :class => "btn btn-default btn-sm", method: :patch do %><i class="icon icon-copy icon-fixed-width"></i> <%= t 'all.verb.duplicate' %><% end %>
          <%= link_to edit_planning_path(planning), :class => "btn btn-primary btn-sm" do %><i class="icon icon-edit icon-fixed-width"></i> <%= t 'all.verb.edit' %><% end %>
          <%= link_to planning, :class => "btn btn-danger btn-sm", method: :delete, data: { confirm: t('all.verb.destroy_confirm') } do %><i class="icon icon-trash icon-fixed-width"></i> <%= t 'all.verb.destroy' %><% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
