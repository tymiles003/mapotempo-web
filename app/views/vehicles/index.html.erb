<h1><%= t '.title' %></h1>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th><%= t 'activerecord.attributes.vehicles.name' %></th>
      <th><%= t 'activerecord.attributes.vehicles.open' %></th>
      <th><%= t 'activerecord.attributes.vehicles.close' %></th>
      <th><%= t 'activerecord.attributes.vehicles.emission' %></th>
      <th><%= t 'activerecord.attributes.vehicles.consumption' %></th>
      <th><%= t 'activerecord.attributes.vehicles.capacity' %></th>
      <th><%= t 'activerecord.attributes.vehicles.color' %></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @vehicles.each do |vehicle| %>
      <tr>
        <td><%= vehicle.name %></td>
        <td><%= if(vehicle.open) then vehicle.open.strftime("%H:%M") end %></td>
        <td><%= if(vehicle.close) then vehicle.close.strftime("%H:%M") end %></td>
        <td><%= vehicle.emission %></td>
        <td><%= vehicle.consumption %></td>
        <td><%= vehicle.capacity %></td>
        <td><div class="vehicle_color" style="background:<%= vehicle.color %>"/></td>
        <td><%= link_to edit_vehicle_path(vehicle) do %><i class="icon-edit icon-fixed-width"></i> <%= t 'all.verb.edit' %><% end %></td>
      </tr>
    <% end %>
  </tbody>
</table>
