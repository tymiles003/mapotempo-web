<% content_for :title, t('.title') %>
<h1><%= t '.title' %></h1>

<%= render 'form' %>

<div id="order_array" class="row">
  <div class="col-md-12">
  </div>
</div>

<div id="dialog-loading" title="<%= t '.dialog.loading.title' %>" style="display:none">
  <div class="dialog-progress">
    <p><%= t '.dialog.loading.in_progress' %></p>
    <div class="progress progress-striped">
      <div class="progress-bar" style="width: 100%"></div>
    </div>
  </div>
</div>

<%
controller.js(
  order_array_id: @order_array.id || 0,
  planning_id: params[:planning_id],
)
%>
